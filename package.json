{
  "name": "paper-paint",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build:fast": "esbuild src/**/*.ts --outdir=dist --platform=node --format=cjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^24.0.4",
    "esbuild": "^0.25.5",
    "typescript": "^5.8.3"
  }
}
